Lists files and directories within the workspace. The path parameter must be an absolute path within the workspace; omit it to use the current workspace directory. Do NOT list directories outside the workspace. You can optionally provide an array of glob patterns to ignore with the ignore parameter. You should generally prefer the Glob and Grep tools, if you know which directories to search.
